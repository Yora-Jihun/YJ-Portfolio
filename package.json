{
    "name": "yj-portfolio",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "concurrently -k -n build,serve -c blue,green \"vite build --watch\" \"vite --open\"",
        "start": "npm run dev",
        "build": "vite build",
        "watch": "vite build --watch"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "concurrently": "^10.0.5",
        "tailwindcss": "^4.0.0",
        "vite": "^8.0.0"
    },
    "dependencies": {
        "alpinejs": "^3.16.3"
    }
}
